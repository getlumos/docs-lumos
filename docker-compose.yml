version: '3.8'

services:
  docs:
    image: ghcr.io/getlumos/docs-lumos:latest
    container_name: docs-lumos
    restart: unless-stopped
    ports:
      - "3001:80"
    environment:
      - NODE_ENV=production
